---
title: 'selfagent desktop, session cards, lander'
description: 'selfagent desktop on main, gdock session cards still short of the mockup, lander still wedges'
date: '2026-09-21T00:00:00.000Z'
authors: ['brianstoker']
tags: ['.plan']
manualCard: true
---

### 2026-09-21

- selfagent desktop: phase 1 on main (engine, surfaces, triggers), then remaining phase 1 packaging against the operator agent db, then a ux rework also on main.
- gdock session cards still aren't the mockup. mapped the live panel so each repo gets a real card and a worktrees node. left-nav still not on main.
- lander still wedges on mono: ghost claims after the feature tree is gone, failed landings left hanging, junk commit messages. repaired the bootstrap. isolated the main deploy runner. sped up release packaging and actions-runner signing.
- hosted the project DAG in RepoView with proportional progress. killed Current Activity lying. brand review defect and brand-index top-up landed.
- Codex shim was prompting an update every load. worktree list JSON was hiding valid trees. grok and dst splash in iterm stopped launching the expected terminal.
- brain hardening and context delivery: prd review first because the format keeps drifting. implementation ran long instead of finishing.
- stopped a recurring macos login-keychain prompt.
